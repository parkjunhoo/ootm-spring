package util;

import java.text.DecimalFormat;

public class Utils {
	static DecimalFormat priceFormat = new DecimalFormat("###,###");
	
	public static priceFormat String(int price) {
		return priceFormat.format(price);
	}
}
